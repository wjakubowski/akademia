#include <iostream>
#define ABC 2
using namespace std;

#define SQR(x) ((x) * (x))

#include "helper.h"
//int sum(int, int);

int main()
{
    cout << "Hello World!" << sum(1,ABC) << '\t' << SQR(1+2) << endl;
    #include "helper.h"
    #include "helper.h"
    return 0;
}
